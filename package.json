{
  "name": "orca-pixel-office",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun test ./test/*.ts",
    "live-verify": "bun run scripts/live-verify.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "bun-types": "^1.1.42",
    "typescript": "^5.7.2"
  }
}
